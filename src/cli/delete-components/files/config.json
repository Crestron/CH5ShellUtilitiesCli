{
  "command": "delete:components",
  "name": "delete:components",
  "aliases": [
    "del:c"
  ],
  "usage": "[options]",
  "options": [
    {
      "key": "list",
      "description": "Prefix for list of component names to be deleted",
      "type": "array",
      "default": [],
      "valueIfNotFound": [],
      "alias": ["-l", "--list"]
    },
    {
      "key": "force",
      "description": "Forces the script to delete the component without asking for a confirmation",
      "type": "boolean",
      "default": true,
      "valueIfNotFound": false,
      "alias": ["-f", "--force"]
    }
  ],
  "additionalHelp": true
}